{
  "name": "final",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd backend && yarn deploy",
    "heroku-prebuild": "yarn install && yarn install --cwd ./frontend && yarn install --cwd ./backend",
    "build": "cd frontend && yarn build"
  }
}
